<% provide(:title, "login") %>

<h1>ログイン</h1>

<div class= "row">
  <div class= "col col-md-6 col-md-offset-3">
    <%= form_with(scope: :session, url: login_path, local: true) do |f| %>
    
      <%= f.label :email, class: "login-label" %>
      <%= f.email_field :email, class: "form-control" %>
    
      <%= f.label :password, class: "login-label" %>
      <%= f.password_field :password, class: "form-control" %>
      
      <%= f.label :remember_me, class: "checkbox inline checkbox-login", for: "remember-me" do %>
        <%= f.check_box :remember_me, id: "remember-me" %>
        <span>ログイン状態を記憶する</span>
      <% end %>
    
      <%= f.submit "ログイン", class: "btn btn-block btn-primary login-btn" %>
    <% end %>
  </div>
</div>

